using College_portal_System.Data.Consts;
using DataAccessLayer.Entities;
using Microsoft.AspNetCore.Identity;

namespace College_portal_System.Seeds
{
    public static class DefaultUsers
    {
        //public static async Task SeedAdminUser(UserManager<ApplicationUser> userManager)
        //{
        //    ApplicationUser admin = new()
        //    {
        //       //Enter The Application User Records
        //    };

        //    var user = await userManager.FindByEmailAsync(admin.Email);

        //    if (user is null)
        //    {
        //        await userManager.CreateAsync(admin, "Enter The Password");
        //        await userManager.AddToRoleAsync(admin, AppRoles.Admin);
        //    }
        //}
    }
}

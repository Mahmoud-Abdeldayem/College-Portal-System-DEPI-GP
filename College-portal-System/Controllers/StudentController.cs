using DataAccessLayer.Entities;
using Microsoft.AspNetCore.Mvc;

namespace College_portal_System.Controllers
{
    public class StudentController : Controller
    {
        //public CollegePortalSystemContext context=new CollegePortalSystemContext();
        //public IActionResult Index(string id)
        //{
        //    id = "1";
        //    Student student = context.Students.FirstOrDefault(x => x.StudentId == id);
        //    return View(student);
        //}
        //public IActionResult Profile(string id)
        //{
        //    Student student = context.Students.FirstOrDefault(x=>x.StudentId==id);
        //    return View("Profile", student);
        //}
    }
}

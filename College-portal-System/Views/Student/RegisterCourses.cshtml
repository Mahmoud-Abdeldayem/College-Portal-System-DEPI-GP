@using College_portal_System.Models.StudentViewModels
@model List<AvailableCoursesViewModel>
@{
    ViewData["Title"] = "RegisterCourses";
    Layout = "~/Views/Shared/_LayoutStudent.cshtml";
    ViewData["firstname"] = "Yassin";
    ViewData["lastname"] = "Ahmed";
    ViewData["image"] = null;
    ViewData["gender"] = "Male";
    ViewData["id"] = "30308132100798";
}

<div class="pagetitle">
    <h1>Profile</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item">Home</li>
            <li class="breadcrumb-item active">Register Courses</li>
        </ol>
    </nav>
</div>
@if (TempData["success"] != null)
{
    <div class="text-danger">
        @TempData["success"]
    </div>
}
<div class="card">
    <div class="card-body">
        <!-- Bordered Table -->
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th scope="col">Course Name</th>
                    <th scope="col">Course Code</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.Name</th>
                        <td>@item.Code</td>
                        <td>
                            <form asp-controller="Student" asp-action="RegisterCourses" asp-route-StudentiD="@ViewData["id"]" asp-route-CourseId="@item.CourseID" method="post">
                                <div class="text-center">
                                    <button type="submit" class="btn btn-primary">Register</button>
                                </div>
                            </form>
                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
</div>

